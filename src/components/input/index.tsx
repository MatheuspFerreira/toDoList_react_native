import React from "react";
import { TextInput, TextInputProps } from "react-native";

type InputTaskProps = TextInputProps;

export function InputTask({ ...rest }: InputTaskProps) {
  return <TextInput {...rest} />
    
      
    
  
}
